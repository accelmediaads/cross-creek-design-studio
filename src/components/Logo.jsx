import logo from '../assets/logo.png'

export default function Logo() {
  return (
    <img
      src={logo}
      alt="Cross Creek Landscape Design + Build"
      style={{ height: 44, width: 'auto' }}
    />
  )
}
